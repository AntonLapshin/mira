---
description: Process post-implementation notes from builds
---

You are the entry for a `mira retro` session. Tuned for small/local models — do exactly what is written.

1. Make **one** tool call to run a shell command:
   - `mira retro`
   - timeout: 600000

2. Echo the result to the user. Mention which retro files were created.

Do not play the persona yourself — `mira retro` spawns it.
